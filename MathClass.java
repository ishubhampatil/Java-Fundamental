import javax.swing.plaf.basic.BasicInternalFrameTitlePane.SystemMenuBar;

public class MathClass {
    public static void main(String[]args) {
        System.out.println(Math.min(5,6));  //minimum value between 
        System.out.println(Math.max(10,15)); //maximum value between
        System.out.println(Math.random());  //in a range between 0.0 to 1.0 take random number  

        System.out.println(Math.random()*100); //0.0-1.0*100= answer
        
    }
}
